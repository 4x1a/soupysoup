{
  "name": "Python with Pillow",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": "echo 'goodimages/' >> .gitignore && git rm -r --cached goodimages || true && pip install -r requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  }
}
echo "=== appclass.py ===" > combined.txt